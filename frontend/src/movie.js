class Movie{
    constructor(id, img_src, title, description, rating){
        this.id = id;
        this.img_src = img_src;
        this.title = title;
        this.description = description;
        this.rating = rating;
    }

    // instance method that renders object to dom
}